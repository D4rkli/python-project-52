[project]
name = "hexlet-code"
version = "0.1.0"
dependencies = [
    "Django>=4.2",
    "gunicorn",
    "psycopg2-binary",
    "python-dotenv",
    "dj-database-url",
    "django-filter",
    "django-bootstrap5",
    "rollbar>=0.16.3",
]

[tool.uv]

